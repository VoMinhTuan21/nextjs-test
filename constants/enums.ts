export enum Gender {
	Male = "male",
	Female = "female",
	Other = "other",
}

export enum Language {
	vi = "vi",
	en = "en",
}

export enum PaymentMethod {
	COD = "CODE",
	MOMO = "MOMO",
}

export enum OrderStatus {
	Pending = "pending",
	Delivering = "delivering",
	Completed = "completed",
	Cancelled = "cancelled",
}
